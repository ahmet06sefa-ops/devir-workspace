<?xml version="1.0" encoding="utf-8"?>
<!--
  v8.6 · Öneri 30: Android 12+ widget kartı.

  ── Düzeltilen sorun ──
  Eskiden burada `@android:color/system_accent2_50` vardı — duvar
  kâğıdından türetilen AÇIK bir ton. Ama v7.67'de kullanıcı
  "widgetlarda karanlık tema kullan" dediği için `values-v31/
  widget_colors.xml` koyu tonlara ayarlanmıştı. Sonuç: renk tanımı
  koyu, kart zemini açık — ikisi çelişiyordu ve Android 12+
  cihazlarda widget yazıları okunmuyordu.

  Artık zemin `@color/w_bg`'den geliyor (yani kullanıcının widget
  tema tercihine saygılı), yalnızca köşe yarıçapı sistemden alınıyor.

  ── system_app_widget_background_radius ──
  Android 12+ launcher'ları widget'ları kendi köşe yarıçapıyla
  kırpıyor. Sabit 26dp verirsek kırpma ile kart köşesi uyuşmuyor ve
  kenarda ince bir hilal kalıyor. Sistem değerini kullanmak bunu
  çözüyor.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <corners android:radius="@android:dimen/system_app_widget_background_radius" />
    <solid android:color="@color/w_bg" />
    <stroke android:width="1dp" android:color="@color/w_stroke" />
</shape>
